server:
  port: 8000

spring:
  application:
    name: config-server
  cloud:
      config:
        server:
          git:
            uri: ssh://git@gitlab.knoviagroup.cn:1022/root/SpringCloudForDotNet.git
            searchPaths: config-server-repo
            lable: master

eureka:
  instance:
    hostname: discovery
    prefer-ip-address: false
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
